// NOTE: 仮の実装。
// コマンドとしてビルド/実行できるように、ここは必ず `package main` とする。
package main

import (
	"fmt"
)

// エントリポイント（ダミー）
// まだ本実装は行わないが、`go run ./cmd/news` やバイナリ生成が失敗しないようにする。
func main() {
	fmt.Println("news コマンド（ダミー）: 実装はこれからです")
}
